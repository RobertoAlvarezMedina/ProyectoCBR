var login-submit = document.getElementById("login-submit");
btn.addEventListener("click", function () {
    renderHTML();
};
function renderHTML() {
    var htmlString = <p>"Guardado con exito"</p>;
  
}